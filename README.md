#FLOFFENHAUSER BREWING CO PRESENTS

Clone the repo
`git clone https://github.com/ahoff314/floffenhauser_brewing.git`

`cd vagrant/catalog`

`vagrant up`
`vagrant ssh`

`cd /vagrant/catalog/`

`python app.py`

